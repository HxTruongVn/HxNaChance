# Testing

*(Chưa viết — khung thư mục đã tạo theo kế hoạch tái tổ chức `docs/`,
chưa có nội dung thật.)*

Hiện có 6 unit test trong `tests/` (`test_align_face`, `test_model_registry`, `test_photo_agent`, `test_runtime_manager`, `test_smoke`, `test_spec_presets`) — chưa có integration test chạy trọn pipeline (xem roadmap/action_items.md #9). CI: `.github/workflows/tests.yml`, hiện chỉ `runs-on: ubuntu-latest`.
