📋 KẾ HOẠCH REFACTOR main_ui.py 
0. CẤU TRÚC REPO THỰC TẾ 
plain
HxNaChance/
├── main.py                    # Entry point — SỬA import ở cuối
├── main_ui.py                 # ← MỤC TIÊU: ~900 dòng, 1 class God Class
├── photo_engine/              # ← PACKAGE, ĐÃ refactor rồi, KHÔNG ĐỤNG
│   ├── __init__.py            # Export: NaChanceEngine, SPEC_PRESETS, PhotoSpec, DEFAULT_PRESET_NAME, _imread_unicode
│   ├── engine.py
│   ├── spec.py
│   ├── utils.py
│   ├── processors/
│   └── analyzers/
├── photo_agent.py             # ← PhotoQAAgent, KHÔNG ĐỤNG
├── print_layout.py            # ← build_layout_canvas, save_layout, LAYOUT_PRESETS, KHÔNG ĐỤNG
├── runtime_manager.py         # ← RuntimeManager + RuntimeReport, KHÔNG ĐỤNG
├── model_registry.py          # KHÔNG ĐỤNG
├── venv_bootstrap.py          # KHÔNG ĐỤNG
├── setup_models.py            # KHÔNG ĐỤNG
├── debug.py                   # KHÔNG ĐỤNG
├── presets/                   # themes.json, layout_presets.json — KHÔNG ĐỤNG
├── api/                       # KHÔNG ĐỤNG
├── tests/                     # Có thể cần sửa import nếu test import main_ui
└── assets/                    # KHÔNG ĐỤNG
1. DEPENDENCY main_ui.py ĐANG DÙNG (không được đổi interface)
Bảng
Đối tượng	Nguồn	Cách dùng trong main_ui.py
NaChanceEngine	photo_engine	self.engine = NaChanceEngine(weights_dir="weights", runtime_report=runtime_report)
SPEC_PRESETS	photo_engine	self.combo_preset = ctk.CTkComboBox(values=list(SPEC_PRESETS.keys()))
PhotoSpec	photo_engine	PhotoSpec(name=..., w=..., h=..., ...)
DEFAULT_PRESET_NAME	photo_engine	Fallback khi preset không khớp
_imread_unicode	photo_engine	Đọc ảnh có dấu tiếng Việt
PhotoQAAgent	photo_agent	self.qa_agent = PhotoQAAgent(self.engine, max_retries=3)
build_layout_canvas	print_layout	build_layout_canvas(src, cfg, append, existing)
save_layout	print_layout	save_layout(canvas, payload, out_path)
LAYOUT_PRESETS	print_layout	for key, preset in LAYOUT_PRESETS.items()
RuntimeReport	runtime_manager	Truyền vào __init__ qua runtime_report param
Nguyên tắc: Tất cả import trên giữ nguyên. Chỉ thay đổi nơi import (từ main_ui.py sang ui/app.py).
2. CẤU TRÚC MỤC TIÊU SAU REFACTOR
plain
HxNaChance/
├── main.py                    # from ui.app import NaChanceApp  (sửa 1 dòng)
├── main_ui.py                 # ← XÓA sau Phase 7
├── ui/                        # ← PACKAGE MỚI
│   ├── __init__.py            # from .app import NaChanceApp
│   ├── app.py                 # NaChanceApp(ctk.CTk) — xương sống + title bar + điều phối
│   ├── theme_manager.py       # ThemeManager — load/validate themes.json
│   ├── config_manager.py      # ConfigManager — đọc/ghi ~/.nachance_ai.json
│   ├── utils.py               # _imwrite_unicode, _open_folder, _safe_float/int
│   ├── side_panel.py          # SidePanel(CTkToplevel) — 1 cửa sổ phụ duy nhất
│   ├── workers/
│   │   └── process_worker.py  # ProcessWorker(Thread) — xử lý ảnh nền
│   ├── tabs/
│   │   ├── process_tab.py     # ProcessTab(CTkFrame) — tab "Xử lý ảnh"
│   │   └── layout_tab.py      # LayoutTab(CTkFrame) — tab "Xếp in"
│   └── dialogs/
│       └── about_dialog.py    # AboutDialog(CTkToplevel)
├── photo_engine/              # ← GIỮ NGUYÊN
├── photo_agent.py             # ← GIỮ NGUYÊN
├── print_layout.py            # ← GIỮ NGUYÊN
└── runtime_manager.py         # ← GIỮ NGUYÊN
3. KẾ HOẠCH THEO PHASE (mỗi phase = 1 PR riêng)
🔷 PHASE 0: Chuẩn bị
Bảng
Hạng mục	Chi tiết
Thời gian	1-2 giờ
File thay đổi	Không
File tạo mới	tests/test_ui_smoke.py (nếu chưa có)
Checklist:
[ ] Chụp màn hình UI hiện tại (cả 2 tab, cả trạng thái thu/phóng)
[ ] Ghi smoke test: chọn file → xử lý → xem preview → xếp in → lưu
[ ] python main.py chạy ổn trên máy dev
[ ] Tạo branch refactor/ui-modular
🔷 PHASE 1: Tách Utilities
Bảng
Thời gian	30 phút
Rủi ro	⭐ Không có
Rollback	Xóa ui/utils.py, paste về main_ui.py
Tạo: ui/utils.py
Python
import os, subprocess, platform, cv2, numpy as np
from pathlib import Path

def _imwrite_unicode(path: str, image: np.ndarray, params=None) -> bool: ...
def _open_folder(path: str) -> None: ...
def _safe_float(val, default=0.0) -> float: ...
def _safe_int(val, default=0) -> int: ...
Sửa: main_ui.py — xóa 4 hàm trên, thay bằng:
Python
from ui.utils import _imwrite_unicode, _open_folder, _safe_float, _safe_int
Test:
bash
python -c "from ui.utils import _imwrite_unicode, _open_folder; print('OK')"
python main.py  # smoke test
🔷 PHASE 2: Tách Theme Manager
Bảng
Thời gian	1 giờ
Rủi ro	⭐ Không có
Rollback	Xóa ui/theme_manager.py, paste _load_themes về main_ui.py
Tạo: ui/theme_manager.py
Python
from pathlib import Path
import json

class ThemeManager:
    REQUIRED_KEYS = ('bg_dark', 'bg_card', 'bg_hover', 'border', ...)
    BUILTIN = {"Dark Blue (mặc định)": {...}}
    
    def __init__(self, presets_dir: Path): ...
    def load(self) -> dict: ...
    @property
    def colors(self) -> dict: ...
    @property
    def names(self) -> list: ...
    def set(self, name: str) -> bool: ...
Sửa: main_ui.py
Xóa _load_themes(), BUILTIN_THEMES_FALLBACK, _REQUIRED_THEME_KEYS
Trong __init__: self.theme_mgr = ThemeManager(Path(__file__).parent / "presets")
self.COLORS = self.theme_mgr.colors (hoặc self.theme_mgr.get(self.theme_name))
🔷 PHASE 3: Tách Config I/O
Bảng
Thời gian	1-2 giờ
Rủi ro	⭐⭐ Thấp
Rollback	Xóa ui/config_manager.py, paste _load_config/_save_config về
Tạo: ui/config_manager.py
Python
from pathlib import Path
import json

class ConfigManager:
    PATH = Path.home() / ".nachance_ai.json"
    
    def load(self) -> dict: ...
    def save(self, data: dict) -> None: ...
    def get_theme(self, default: str) -> str: ...
    def get_save_dir(self, default: str) -> str: ...
    def get_layout(self) -> dict: ...
Sửa: main_ui.py
Xóa _load_config, _save_config, _load_theme_name
Trong __init__: self.cfg_mgr = ConfigManager()
Tạo hàm ngắn self._apply_config(cfg) để set giá trị widget từ dict (~20 dòng)
Test:
Xóa ~/.nachance_ai.json → mở app → config mặc định OK
Đổi thư mục lưu/theme → restart → nhớ đúng
🔷 PHASE 4: Tách Side Panel
Bảng
Thời gian	2-3 giờ
Rủi ro	⭐⭐ Thấp
Rollback	Xóa ui/side_panel.py, paste các hàm _build_side_panel về
Tạo: ui/side_panel.py
Python
import customtkinter as ctk
from PIL import Image as PILImage
import cv2

class SidePanel(ctk.CTkToplevel):
    MODE_ORIENT = 'orient'
    MODE_RESULT = 'result'
    MODE_LAYOUT = 'layout'
    
    def __init__(self, master, colors: dict, fonts: dict): ...
    def set_colors(self, colors: dict): ...           # thay _restyle_side_panel
    def show_orientation(self, image, filename, idx, total, on_confirm, on_skip, on_cancel): ...
    def show_result(self, image): ...
    def show_layout(self, canvas, width, height): ...
    def hide(self): ...
    def sync_position(self, main_x, main_y, main_w): ...
Sửa: main_ui.py
Xóa: _build_side_panel, _restyle_side_panel, _sync_side_panel_position, _show_side_panel, _hide_side_panel, _preview_render_current, _preview_set_rotation, _preview_rotated_image
Trong __init__: self.side_panel = SidePanel(self, self.COLORS, self._fonts)
Bind <Configure> gọi self.side_panel.sync_position(...)
Test:
Xác nhận chiều ảnh (0/90/180/270) → xử lý → xem preview → xem bản in
Kéo cửa sổ chính → side panel bám theo
🔷 PHASE 5: Tách Process Worker
Bảng
Thời gian	2-3 giờ
Rủi ro	⭐⭐⭐ Trung bình (threading)
Rollback	Xóa ui/workers/process_worker.py, paste hàm process inline về
Tạo: ui/workers/process_worker.py
Python
import threading
from datetime import datetime
import os, cv2
from ui.utils import _imwrite_unicode

class ProcessWorker(threading.Thread):
    def __init__(self, engine, qa_agent, files, spec, bg_color, options, quality, save_dir,
                 on_progress, on_done):
        super().__init__(daemon=True)
        ...
    
    def run(self):
        for i, path in enumerate(self.files):
            self.on_progress(i + 1, len(self.files))
            agent_result = self.qa_agent.process(path, self.spec, self.bg_color, self.options)
            ...
        self.on_done(results)
Sửa: main_ui.py — _process_files chỉ còn:
Python
def _process_files(self, files):
    self._set_busy(True)
    worker = ProcessWorker(
        self.engine, self.qa_agent, files,
        self._get_spec(), self._get_bg_color(), self._get_options(), quality, self.save_dir,
        on_progress=lambda idx, total: self.after(0, lambda: self.status.configure(...)),
        on_done=lambda results: self.after(0, lambda: self._on_process_done(results))
    )
    worker.start()
🔷 PHASE 6: Tách Layout Tab
Bảng
Thời gian	2 giờ
Rủi ro	⭐⭐⭐ Trung bình
Rollback	Xóa ui/tabs/layout_tab.py, paste _build_layout_tab về
Tạo: ui/tabs/layout_tab.py
Python
import customtkinter as ctk
from tkinter import filedialog, messagebox
from print_layout import build_layout_canvas, save_layout, LAYOUT_PRESETS

class LayoutTab(ctk.CTkFrame):
    def __init__(self, master, colors: dict, fonts: dict, on_preview, on_save, on_print): ...
    def get_config(self) -> dict: ...
    def set_source(self, path: str): ...
    def get_source(self) -> str: ...
Sửa: main_ui.py
Xóa _build_layout_tab, _get_layout_config, _build_layout, _render_layout_preview, _layout_preview, _layout_save, _layout_print, _layout_live_refresh
Trong _build_main_panel: self.tab_layout = LayoutTab(...)
🔷 PHASE 7: Tách Process Tab + App Core (CUỐI CÙNG)
Bảng
Thời gian	3-4 giờ
Rủi ro	⭐⭐⭐⭐ Cao
Rollback	Giữ main_ui.py.bak, đổi tên lại
Tạo mới:
ui/tabs/process_tab.py — ProcessTab(CTkFrame)
ui/dialogs/about_dialog.py — AboutDialog(CTkToplevel)
ui/app.py — NaChanceApp(ctk.CTk) chỉ còn xương sống
Sửa: main.py
Python
# Cũ:
from main_ui import NaChanceApp

# Mới:
from ui.app import NaChanceApp
Xóa: main_ui.py
Test (Full Regression):
[ ] App khởi động → không lỗi console
[ ] Toggle panel (thu/phóng)
[ ] Chọn file → xử lý → preview → xếp in → lưu → mở thư mục
[ ] Chọn thư mục → batch
[ ] Xác nhận chiều ảnh (0/90/180/270)
[ ] Đổi theme → restart → theme mới
[ ] Đổi thư mục lưu → nhớ
[ ] Lite Mode → checkbox AI bị disable
[ ] About dialog
4. NGUYÊN TẮC "KHÔNG ĐỤNG" (ĐÃ VERIFY)
Bảng
File/Package	Tình trạng	Lý do
photo_engine/	GIỮ NGUYÊN	Đã là package đúng chuẩn, __init__.py export facade
photo_agent.py	GIỮ NGUYÊN	PhotoQAAgent độc lập
print_layout.py	GIỮ NGUYÊN	Engine xếp ảnh độc lập
runtime_manager.py	GIỮ NGUYÊN	RuntimeManager + RuntimeReport không đổi
main.py	SỬA 1 DÒNG IMPORT	from ui.app import NaChanceApp
presets/themes.json	GIỮ NGUYÊN	Theme data
presets/layout_presets.json	GIỮ NGUYÊN	Layout data
~/.nachance_ai.json	GIỮ NGUYÊN	User config
5. ROLLBACK CHIẾN LƯỢC
bash
# Nếu phase nào fail:
git checkout main_ui.py          # khôi phục file gốc
git checkout main.py             # khôi phục import
rm -rf ui/                       # xóa module mới
# photo_engine/, photo_agent.py, print_layout.py, runtime_manager.py 
# không bị đụng nên không cần khôi phục
