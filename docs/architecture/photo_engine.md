# Kiến trúc `photo_engine/`

*(Chưa viết — khung thư mục đã tạo theo kế hoạch tái tổ chức `docs/`,
chưa có nội dung thật.)*

Mô tả package đã tách từ `photo_engine.py` monolith (spec.py, utils.py, analyzers/, processors/, engine.py) — nội dung chi tiết từng bước tách nằm trong lịch sử git (đã bị xoá khỏi docs/ theo quy ước cũ, xem docs/archive/README.md).
