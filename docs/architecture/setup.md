# Kiến trúc `setup/`

*(Chưa viết — khung thư mục đã tạo theo kế hoạch tái tổ chức `docs/`,
chưa có nội dung thật.)*

Mô tả `venv_bootstrap.py`, `runtime_manager.py`, `installer.py`, `setup_models.py`, `debug.py` và quan hệ giữa chúng (`__init__.py` import tương đối, còn `setup_models.py`/`debug.py` phải tự thêm project root vào sys.path để chạy trực tiếp được — xem lịch sử fix trong git log).
