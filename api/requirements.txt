# FastAPI service — cài từ thư mục gốc repo:
#   pip install -r api/requirements.txt
#
# File này kéo theo toàn bộ dependency AI/desktop (requirements.txt).
-r requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0
