# Core — không gồm torch (xem requirements-torch-cpu.txt) và AI packages
# (xem requirements-ai.txt). Dùng để cài riêng cho máy CPU yếu, tránh tải
# nhầm bản torch có bundle CUDA runtime (rất nặng, không cần nếu không có GPU).
opencv-contrib-python>=4.8.0
Pillow>=10.0.0
# Pin numpy < 2.0: numpy 2.x gây lỗi ABI với torch/torchvision/basicsr đã
# compile sẵn cho numpy 1.x (RuntimeError: Numpy is not available).
numpy>=1.24.0,<2.0.0
mediapipe>=0.10.0,<0.10.15
rembg>=2.0.50
onnxruntime>=1.15.0

# Utilities
tqdm>=4.65.0
requests>=2.31.0
gdown>=4.7.0
scipy>=1.10.0

# UI
customtkinter>=5.2.0
