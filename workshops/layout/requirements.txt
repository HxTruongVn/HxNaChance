# Xưởng Xếp in (workshops/layout/) — requirements riêng, Xưởng tự quản.
# Cài trực tiếp: pip install -r workshops/layout/requirements.txt
# (setup/requirements.txt gom tất cả Xưởng lại — dùng file đó nếu chạy cả app.)
#
# print_layout.py chỉ dùng PIL để ghép ảnh
# vào khổ in, không cần torch/opencv/AI model nào (đã grep xác nhận toàn
# bộ import trong workshops/layout/*.py: chỉ stdlib + Pillow).

Pillow>=10.0.0

# UI (tab "Xếp in" trong workshops/layout/ui.py dùng trực tiếp)
customtkinter>=5.2.0
